<!--components/sparklist/sparklist.wxml-->
<view class="playlist-container" bind:tap="goToList">
  <image src="{{playlist.picUrl}}" class="playlist-img"></image>
  <text class="playlist-count">{{_count}}</text>
  <view class="playlist-name">{{playlist.name}}</view>
</view>
